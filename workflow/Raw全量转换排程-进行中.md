---
type: topic
created: 2026-06-08
updated: 2026-06-08
tags: [进行中, Raw, 转换排程, 派生层]
---

# Raw 全量转换排程 - 进行中

## 当前目标

按照 [[Raw全量转换与长期派生层工作流]]，把 `raw/` 中的原始文件逐批沉淀为长期可复用的 `.derived/` 派生层。

## 当前执行状态

- 当前进行中批次：`raw/中国营养学会`
- 当前任务 `job_id`：`derive-20260608-013033`
- 启动时间：`2026-06-08 01:30 +08:00`
- 当前策略：本地后台持续运行，按 `10` 页写一次轻量进度
- 轻量检查命令：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\check_local_derive_job.ps1 -JobId derive-20260608-013033`

## 排程使用方式

- 本排程就是 Raw 全量转换的执行入口，可以直接使用
- 推荐流程是：
  - 先更新本排程
  - 再按本排程启动本地长任务
  - 每完成一批后回写本排程状态
- 如果 `raw/` 的目录结构、文件类型或优先级发生变化，应先改排程，再继续执行
- 不建议跳过排程，直接把整个 `raw/` 作为单个大任务启动

## 当前规模

- 总文件数：`184`
- PDF：`163`
- DOCX：`13`
- DOC：`5`
- EPUB：`2`
- JPG：`1`

## 推荐推进顺序

### 第一阶段：先吃掉高价值 PDF 主体

- [ ] `raw/中国营养学会`：47（当前进行中：`derive-20260608-013033`）
- [ ] `raw/中华临床营养杂志2025年4期`：13
- [ ] `raw/营养书籍`：33
- [ ] `raw/减肥营养书`：12 PDF
- [ ] `raw/仰望尾迹云课程`：3 PDF

### 第二阶段：课程与业务资料

- [ ] `raw/私域运营课`：8 PDF，9 DOCX，4 DOC
- [ ] `raw/范一晴 商业陪跑课`：3 PDF
- [ ] `raw/商业职场`：20 PDF

### 第三阶段：人群与方法论资料

- [ ] `raw/高敏感减肥指导定位`：4 DOCX，1 DOC
- [ ] `raw/高敏感人群`：7 PDF
- [ ] `raw/心理学`：8 PDF
- [ ] `raw/教练思维`：2 PDF
- [ ] `raw/个人成长`：7 PDF
- [ ] `raw/我的减肥指导`：1 PDF

### 第四阶段：补齐非 PDF 文件

- [ ] `raw/中国营养学会`：1 JPG
- [ ] `raw/减肥营养书`：2 EPUB
- [ ] 所有 DOC 与 DOCX 的统一派生目录整理

## 当前执行原则

- 已有稳定成功链路的类型优先进入主流程：PDF、DOCX、JPG、EPUB
- 当前需要跳过的类型：DOC
- “全量转换”在当前阶段的定义是：
  - 支持格式生成派生产物
  - 跳过格式生成跳过记录
- 批量执行优先按顶层目录或排程批次启动，不把整个 `raw/` 一次性混跑

## 每批完成标准

- [ ] 本目录下所有文件都有对应派生目录
- [ ] 支持格式文件至少生成一份 Markdown 正文
- [ ] 每个文件至少生成一份结构化结果、跳过记录或异常说明
- [ ] 路径能从 `.derived/` 反查回 `raw/`
- [ ] 本批完成后再进入下一批，不跨目录并发扩散

## 当前建议起点

第一批建议从 `raw/中国营养学会` 开始，理由是：

- 文件数量最多，但格式最单一，主要是 PDF
- 后续对 `wiki/` 的复用价值最高
- 最适合先验证“长期派生层”是否稳定

## 执行备注

- 本排程只覆盖 `.derived/` 派生层产物
- 转换层完成后应在 `.derived/` 校验通过处结束
- PDF 优先沿用现有 `.derived/pdf_extract/` 规则
- 长任务优先使用本地后台运行与轻量进度文件，不要求 Agent 持续监控
- 如果某个单文档任务超过 `10` 分钟未完成，允许每 `10` 分钟检查一次轻量进度，确认没有卡死
- 非 PDF 先按工作流规划派生目录，后续再统一命令收口
- 异常文件不跳过，必须留痕
- 每次正式执行前，先确认本排程已更新到当前目录现状

## 关联

- [[Raw全量转换与长期派生层工作流]]
- [[PDF本地解析工具链]]
